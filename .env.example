# ── Autonomous CI/CD Healing Agent — Environment Configuration ──

# PostgreSQL connection string (Supabase or any Postgres 14+ instance).
# Example: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL=

# Set to 'false' to disable SSL (e.g. for local PostgreSQL).
# DB_SSL=false

# GitHub personal access token for pushing fix branches.
# GITHUB_TOKEN=

# Server port (defaults to 8080).
# PORT=8080

# Optional: if the frontend is hosted separately from the backend.
# Leave empty for local dev (Vite proxy will route /api to the backend).
# VITE_API_BASE_URL=https://your-backend.example.com

# Maximum retry iterations per healing run (defaults to 5, max 20).
# AGENT_RETRY_LIMIT=5
